name: restaurant-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - sqlite
  - pip:
      - fastapi
      - fastmcp
      - uvicorn
      - openai
      - requests
      - python-dotenv
      - pandas
